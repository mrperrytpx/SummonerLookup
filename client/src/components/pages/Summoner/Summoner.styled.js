import { bodyHeight } from "consts/cssVals";
import styled from "styled-components";

export const StyledSummoner = styled.div`
    width: min(1200px, 98%);
    margin: auto;
    min-height: ${bodyHeight};
`;
