let accessToken = "";

// access token setter function
export const setAccessToken = (string) => {
    accessToken = string;
}

// access token getter function
export const getAccessToken = () => {
    return accessToken;
}